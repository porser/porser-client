import createTheme from "@sonnat/ui/styles/createTheme";

const theme = createTheme({
  direction: "rtl",
  colors: { primary: "#f56468", secondary: "#27184f" },
  custom: {}
});

export default theme;
